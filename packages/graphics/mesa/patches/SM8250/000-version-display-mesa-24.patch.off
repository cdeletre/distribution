diff --git a/meson.build b/meson.build
index 07991a6eb5e..743d7435ad8 100644
--- a/meson.build
+++ b/meson.build
@@ -53,6 +53,7 @@ pre_args = [
   '-D__STDC_LIMIT_MACROS',
   '-DPACKAGE_VERSION="@0@"'.format(meson.project_version()),
   '-DPACKAGE_BUGREPORT="https://gitlab.freedesktop.org/mesa/mesa/-/issues"',
+  '-DVERSION_DISPLAY="@0@"'.format(get_option('version-display')),
 ]
 # Arguments for c or cpp compiler, can be compiler options
 c_cpp_args = []
diff --git a/meson_options.txt b/meson_options.txt
index 84e0f20dcfd..8a4b6c5c582 100644
--- a/meson_options.txt
+++ b/meson_options.txt
@@ -1,6 +1,13 @@
 # Copyright © 2017-2019 Intel Corporation
 # SPDX-License-Identifier: MIT
 
+option(
+  'version-display',
+  type : 'string',
+  value : '',
+  description : 'Version to display in the freedreno device name'
+)
+
 option(
   'split-debug',
   type : 'feature',
diff --git a/src/freedreno/vulkan/tu_device.cc b/src/freedreno/vulkan/tu_device.cc
index a6e284f11c0..6d162406742 100644
--- a/src/freedreno/vulkan/tu_device.cc
+++ b/src/freedreno/vulkan/tu_device.cc
@@ -1272,7 +1272,7 @@ tu_physical_device_init(struct tu_physical_device *device,
    if (strncmp(fd_name, "FD", 2) == 0) {
       device->name = vk_asprintf(&instance->vk.alloc,
                                  VK_SYSTEM_ALLOCATION_SCOPE_INSTANCE,
-                                 "Turnip Adreno (TM) %s", &fd_name[2]);
+                                 "Turnip Adreno (TM) %s %s", &fd_name[2], VERSION_DISPLAY);
    } else {
       device->name = vk_strdup(&instance->vk.alloc, fd_name,
                                VK_SYSTEM_ALLOCATION_SCOPE_INSTANCE);
